'use client'

import './globals.css'

export default function NotFound() {
    return (
        <main className="not-found flex min-h-screen flex-col items-center">
            <h1>Not found.</h1>
            <p>Unfortunately, we could not find the requested page or resource.</p>
        </main>
    )
}